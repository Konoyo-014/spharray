# Notebook Tutorials

Open `getting_started.ipynb` from the repository root after installing the
development environment. The notebook mirrors the command-line tutorials, but it
keeps each step in a separate cell for interactive inspection.

```bash
jupyter lab examples/notebooks/getting_started.ipynb
```
